// Operador condicional ternário
let pao = true
let queijo = true

const niceBreackfast = pao && queijo ? 'café top' : 'cafe mais ou memos'
console.log(niceBreackfast)


const niceUBreackfast = pao || queijo ? 'café top' : 'cafe mais ou memos'
console.log(niceUBreackfast)

// Verificar a idade
let age = 19
const canDriver = age > 18 ? 'Pode dirigir' : 'Não pode dirigir'
console.log(canDriver)